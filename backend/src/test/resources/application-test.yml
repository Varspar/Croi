spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/croi_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

jwt:
  secret: test-secret-key-for-jwt-signing-minimum-32-characters
  expiration-ms: 3600000
