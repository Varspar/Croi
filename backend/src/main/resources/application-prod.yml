spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
  flyway:
    clean-disabled: true

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    root: WARN
    com.croi: INFO
