# Public reCAPTCHA v3 site key (safe to expose to the browser — it's the
# public half of the key pair). Get one at https://www.google.com/recaptcha/admin
# Register the site as reCAPTCHA v3.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
